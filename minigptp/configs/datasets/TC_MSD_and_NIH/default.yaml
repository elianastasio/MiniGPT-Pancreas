datasets:
  tc_msd_and_tcia:
    # data_dir: ${env.data_dir}/datasets
    data_type: images # [images|videos|features]

    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          url: data/Scripts/A3/A3_TC_train_merged.json
          storage: data/Scripts/A3/A3_TC_train_merged.json
      images:
          storage: data/A3_datasets